export interface Testimonial {
  name: string;
  text: string;
  avatar: string;
}

export interface FaqItem {
  q: string;
  a: string;
}

export interface SalesNotification {
  id: number;
  name: string;
  pkg: string;
}
